import 'package:flutter/material.dart';

class AdminAdsControl extends StatelessWidget {
  const AdminAdsControl({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(centerTitle: true, title: Text("Admin Ads Control")),
      body: Center(child: Text("Admin Ads Control")),
    );
  }
}
