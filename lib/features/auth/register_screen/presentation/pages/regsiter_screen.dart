import 'package:flutter/material.dart';

class RegsiterScreen extends StatelessWidget {
  const RegsiterScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Regsiter Screen")),
    );
  }
}
